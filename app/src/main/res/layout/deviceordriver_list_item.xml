<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <LinearLayout
        android:id="@+id/deviceordriver_row"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:paddingStart="52dp">

        <TextView
            android:id="@+id/deviceordriver_name"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginStart="12dp"
            android:layout_marginTop="12dp"
            android:layout_marginEnd="12dp"
            android:textAppearance="@style/TextAppearance.AppCompat.Medium" />

        <TextView
            android:id="@+id/deviceordriver_type"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginStart="20dp"
            android:layout_marginEnd="20dp"
            android:layout_marginBottom="12dp"
            android:textAppearance="@style/TextAppearance.AppCompat.Small" />

    </LinearLayout>

    <ImageView
        android:id="@+id/deviceordriver_more"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:paddingStart="8dp"
        android:paddingEnd="8dp"
        android:paddingTop="4dp"
        android:layout_alignTop="@id/deviceordriver_row"
        android:layout_alignEnd="@id/deviceordriver_row"
        app:srcCompat="@drawable/ic_menu_overflow" />


    <ImageView
        android:id="@+id/deviceordriver_status_disconnected"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:paddingTop="8dp"
        android:visibility="gone"
        android:layout_alignTop="@id/deviceordriver_row"
        android:layout_alignStart="@id/deviceordriver_row"
        app:srcCompat="@drawable/ic_bluetooth_disconnected" />
    <ImageView
        android:id="@+id/deviceordriver_status_connected"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:paddingTop="8dp"
        android:visibility="gone"
        android:layout_alignTop="@id/deviceordriver_row"
        android:layout_alignStart="@id/deviceordriver_row"
        app:srcCompat="@drawable/ic_bluetooth_connected" />

</RelativeLayout>